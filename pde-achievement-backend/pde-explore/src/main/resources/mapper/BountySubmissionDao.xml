<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.meituan.pde.dao.BountySubmissionDao">

    <resultMap id="BaseResultMap" type="com.meituan.pde.entity.BountySubmission">
        <id column="id" property="id"/>
        <result column="task_id" property="taskId"/>
        <result column="phase" property="phase"/>
        <result column="user_mis" property="userMis"/>
        <result column="title" property="title"/>
        <result column="url" property="url"/>
        <result column="repo_url" property="repoUrl"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="reject_reason" property="rejectReason"/>
        <result column="is_winner" property="isWinner"/>
        <result column="vote_count" property="voteCount"/>
        <result column="deleted_flag" property="deletedFlag"/>
        <result column="add_time" property="addTime" jdbcType="TIMESTAMP"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, task_id, phase, user_mis, title, url, repo_url, description, status,
        reject_reason, is_winner, vote_count, deleted_flag, add_time, create_time, update_time
    </sql>

    <select id="findByTaskIdAndPhase" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM bounty_submission
        WHERE task_id = #{taskId} AND phase = #{phase} AND deleted_flag = 0
        ORDER BY vote_count DESC, id ASC
    </select>

    <select id="findByTaskIdAndPhaseAndUser" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM bounty_submission
        WHERE task_id = #{taskId} AND phase = #{phase} AND user_mis = #{userMis} AND deleted_flag = 0
        LIMIT 1
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM bounty_submission
        WHERE id = #{id} AND deleted_flag = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO bounty_submission (task_id, phase, user_mis, title, url, repo_url, description)
        VALUES (#{taskId}, #{phase}, #{userMis}, #{title}, #{url}, #{repoUrl}, #{description})
    </insert>

    <update id="update">
        UPDATE bounty_submission
        SET title = #{title}, url = #{url}, repo_url = #{repoUrl}, description = #{description}
        WHERE id = #{id} AND deleted_flag = 0
    </update>

    <update id="incrementVoteCount">
        UPDATE bounty_submission SET vote_count = vote_count + 1 WHERE id = #{id} AND deleted_flag = 0
    </update>

    <update id="decrementVoteCount">
        UPDATE bounty_submission SET vote_count = GREATEST(vote_count - 1, 0) WHERE id = #{id} AND deleted_flag = 0
    </update>

    <update id="updateReview">
        UPDATE bounty_submission
        SET status = #{status}, reject_reason = #{rejectReason}
        WHERE id = #{id} AND deleted_flag = 0
    </update>

    <select id="countByTaskId" resultType="int">
        SELECT COUNT(DISTINCT user_mis) FROM bounty_submission
        WHERE task_id = #{taskId} AND deleted_flag = 0
    </select>

    <!-- 设置winner：先清除同任务同阶段的winner，再设置新winner -->
    <update id="setWinner">
        UPDATE bounty_submission
        SET is_winner = CASE WHEN id = #{id} THEN 1 ELSE 0 END
        WHERE task_id = #{taskId} AND phase = #{phase} AND deleted_flag = 0
    </update>

    <!-- 批量查询某用户在指定 task 列表中的提交记录 -->
    <select id="findByUserMisAndTaskIds" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM bounty_submission
        WHERE user_mis = #{userMis} AND deleted_flag = 0
        AND task_id IN
        <foreach collection="taskIds" item="tid" open="(" separator="," close=")">
            #{tid}
        </foreach>
    </select>

</mapper>
