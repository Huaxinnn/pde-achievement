<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.meituan.pde.dao.StageStepDao">
    <resultMap id="BaseResultMap" type="com.meituan.pde.entity.StageStep">
        <id column="id" property="id"/>
        <result column="stage_id" property="stageId"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="title" property="title"/>
        <result column="description" property="description"/>
        <result column="commands" property="commands"/>
        <result column="tips" property="tips"/>
        <result column="add_time" property="addTime"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, stage_id, sort_order, title, description, commands, tips, add_time, create_time
    </sql>

    <select id="findByStageId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM stage_step
        WHERE stage_id = #{stageId} AND deleted_flag = 0
        ORDER BY sort_order ASC
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM stage_step
        WHERE id = #{id} AND deleted_flag = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO stage_step (stage_id, sort_order, title, description, commands, tips)
        VALUES (#{stageId}, #{sortOrder}, #{title}, #{description}, #{commands}, #{tips})
    </insert>

    <update id="update">
        UPDATE stage_step
        SET stage_id = #{stageId}, sort_order = #{sortOrder}, title = #{title},
            description = #{description}, commands = #{commands}, tips = #{tips}
        WHERE id = #{id}
    </update>
</mapper>